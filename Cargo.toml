[package]
name = "kish_dama_studio"
version = "0.1.0"
edition = "2021"

[dependencies]
kish = "1.1.7"
eframe = { version = "0.34.2", default-features = false, features = ["default_fonts", "glow"] }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
